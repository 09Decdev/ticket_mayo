# Gateway-auth origin (register + login). KHONG hardcode.
VITE_GATEWAY_AUTH_URL=http://localhost:3000

# ticket-mayo backend origin (dev direct). Client auto-appends /ticket-mayo prefix.
# Gateway-proxy deploy: dat = origin gateway (vd https://gateway.local) — client van append /ticket-mayo.
VITE_TICKET_API_BASE=http://localhost:3005

# Link tải app — trang fallback khi user chưa cài (universal link /c/:token mở app nếu đã cài).
VITE_APP_DOWNLOAD_URL=
